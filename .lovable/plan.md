## المشكلة
الزر في `/admin` يشير إلى `/admin_/pulse` (مسار داخلي)، مما يسبب 404. عنوان URL الصحيح في المتصفح هو `/admin/pulse` (الشرطة السفلية تُحذف من URL).

## الإصلاح
في `src/routes/admin.tsx` (السطر ~59): تغيير `to="/admin_/pulse"` إلى `to="/admin/pulse"`.

لا تغييرات أخرى مطلوبة.